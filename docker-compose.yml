services:
  backend:
    build: ./backend
    ports:
      - "4000:4000"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    ports:
      - "3001:80"
    depends_on:
      - backend

  admin:
    build: ./admin
    ports:
      - "5173:80"
    depends_on:
      - backend